#!/usr/bin/make -f
# -*- makefile -*-

build:
	make PREFIX=/usr RUN=/var/run SRCTAB=/etc/srctab
	make install

build-arch build-indep binary-arch binary-indep: build

binary: binary-arch binary-indep

clean:
	rm -f upsource sourcetab.awk .prefix .run .srctab

install:
	make install
