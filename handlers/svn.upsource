MOUNT=$1
SOURCE=$2
if test -d $MOUNT/.svn; then
   cd $1; svn update;
else
   svn checkout $SOURCE $MOUNT;
fi;
if "${OWNER}" != ""; then
   chown -R ${OWNER} ${MOUNT};
fi
if "${GROUP}" != ""; then
   chgrp -R ${GROUP} ${MOUNT};
fi
if "${MODE}" != ""; then
   chmod -R ${MODE} ${MOUNT};
fi
