MOUNT=$1
SOURCE=$2
if test -d $MOUNT/.git; then
   cd $1; git pull;
else
   git clone $SOURCE $MOUNT;
fi;
if "${OWNER}" != ""; then
   chown -R ${OWNER} ${MOUNT};
fi;
if "${GROUP}" != ""; then
   chgrp -R ${GROUP} ${MOUNT};
fi;
if "${MODE}" != ""; then
   chmod -R ${MODE} ${MOUNT};
fi;
